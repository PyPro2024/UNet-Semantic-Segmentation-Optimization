# UNet-Semantic-Segmentation-Optimization
A semantic segmentation project using the U-Net architecture in TensorFlow/Keras. Features experiments with model depth (encoder/decoder layers) and learning rates to optimize human-background separation accuracy.
